from lit.llvm import llvm_config

if config.build_iree_backend:
  llvm_config.with_environment('PYTHONPATH', [
    os.path.join(config.cancer_obj_root),
  ], append_path=True)
else:
  config.unsupported = True
